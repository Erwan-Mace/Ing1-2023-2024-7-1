#include <stdio.h>
#include <unistd.h> // pour la fonction sleep(1)
#include <stdlib.h> // pour la fonction system("cls")

int main() {
    int secondes = 120; // Temps imparti pour la partie
    
    while (secondes > 0) { // Tant que le nombre de secondes n'a pas atteint 0
        printf("Temps restant : %d secondes", secondes); // Afficher le nombre de secondes restantes
        sleep(1); // Attendre 1 seconde
        secondes--; // Le nombre de secondes diminue de 1 à chaque boucle
        system("cls"); // Nettoyer la console
    }
    
    if (secondes == 0) { // Quand il ne reste plus de temps
        printf("Time's up!\n");
        sleep(1); // Attendre 1 seconde pour pouvoir lire
    }
    
    return 0;
}
